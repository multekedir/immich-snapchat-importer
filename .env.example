# Immich Snapchat Importer Configuration
# Copy this file to .env and fill in your values
#
# Usage:
#   cp .env.example .env
#   nano .env  # Edit with your values

# ============================================
# Required: Immich Server Configuration
# ============================================
# Your Immich server API URL
# Format: http://immich-server:2283/api
# For Docker: Use service name (e.g., immich-server)
# For local: Use localhost (e.g., http://localhost:2283/api)
IMMICH_URL=http://immich-server:2283/api

# Your Immich API Key
# Get it from: Immich → Account Settings → API Keys → Create API Key
IMMICH_API_KEY=your_immich_api_key_here

# ============================================
# Optional: Download Settings
# ============================================
# Delay between downloads (in seconds)
# Lower = faster but may trigger rate limiting
# Higher = slower but safer (recommended: 2.0-5.0)
DELAY=2.0

# ============================================
# Optional: Processing Settings
# ============================================
# Overlay opacity multiplier (default: 1.3)
# OVERLAY_OPACITY=1.3

# Shadow offset in pixels (default: 2)
# SHADOW_OFFSET=2

# ============================================
# Notes
# ============================================
# - All values can be overridden in docker-compose.yml
# - Web interface allows per-import configuration
# - CLI arguments take highest priority
